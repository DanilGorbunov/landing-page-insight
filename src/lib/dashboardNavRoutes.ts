/** Section ids for `/full-insights?section=` — not separate routes like `history` or `monitor`. */
export const FULL_INSIGHTS_SECTION_IDS = new Set([
  "overview",
  "compare",
  "performance",
  "actions",
  "copy",
  "hints",
  "beat",
]);
